<%= form_for(current_user.active_solicitations.find_by(user_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= f.submit "Cancelar Pedido", class: "btn" %>
<% end %>